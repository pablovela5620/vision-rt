# Rust build artifacts
/target/

# TensorRT serialized engines — binary and machine-specific, must not be committed
*.engine

# Model weights — distributed via model hub, never in-repo
*.onnx
*.onnx.data
*.pt
models/

# Debug / output images at repo root
/*.jpg
/*.png

# Environment variables
.env

# nested / workspace-excluded example crates build into their own target/
**/target/
.pixi/
.pixi-trt/
